---
title: init
weight: 1
---

# init

Create the default config file at `~/.git-sync/config.toml` (or the path given by `--config`).

```bash
git-sync init [--config <path>]
```

Does nothing if the file already exists.
